{
  "$id": "Shared.json",
  "definitions": {
    "message_type": {
      "type": "string",
      "enum": [
        "Clinic",
        "State",
        "Practitioner",
        "Patient",
        "Entry",
        "Entry Attribute",
        "Patient Practitioner"
      ]
    },
    "id": {
      "type": "string"
    },
    "emr_id": {
      "type": "string",
      "readOnly": true,
      "description": "The unique identifier of the source record within the EMR."
    },
    "operation": {
      "type": "string",
      "enum": ["active"]
    }
  }
}
